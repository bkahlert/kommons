@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Vomit : Category() {
    val `(хдх)` by auto()
    val `（´＊｀）` by auto()
    val `（≧ｍ≦）` by auto()
    val `（＾～＾）` by auto()
    val `(´π`)` by auto("(´π`)")
    val `(`A`)` by auto("('A`)")
    val `（＊Л＊）` by auto()
    val `（●≧艸≦` by auto()
    val `（＞３＜）三` by auto("（>３<）三")
    val `(;´Д`)` by auto("(;´Д`)")
    val `( -․-)` by auto("( -.-)")
    val `(￣x￣;)` by auto("(￣x￣;)")
    val `( ×m×)` by auto()
    val `（･□･）川` by auto()
    val `（´艸｀＊）` by auto()
    val `＿|￣|○､;` by auto("＿|￣|○､;")
    val `（。－＜＞ー。）` by auto("（。－<>ー。）")
    val `(　´З｀)＝３` by auto()
    val `（～Д～）＊＊＊` by auto()
    val `σ(　`　┰￣)` by auto()
    val `( ﾟ ３ﾟ)≡@` by auto()
    val `（（（○∀○）））` by auto()
    val `(；－з-)＝゜。` by auto("(；－з-)＝゜。")
    val `((〇＜*`_、′` by auto("((〇<*`_、′")
    val `ΣΣΣΣΣ（０＜）` by auto("ΣΣΣΣΣ（０＜）")
    val `（;ﾟ;艸;ﾟ;）` by auto("（;ﾟ;艸;ﾟ;）")
    val `｀．゜∵(゜∀゜)` by auto()
    val `_(´ཀ`」 ∠)_` by auto("_(´ཀ`」 ∠)_")
    val `(*●艸ЗU$)◆◇` by auto()
    val `(o`ｪﾟ)﹕;*․` by auto("(o'ｪﾟ):;*.")
}
