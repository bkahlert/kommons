@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Scared : Category() {
    val `（ΩДΩ）` by auto()
    val `(O∆O)` by auto()
    val `ゞ◎Д◎ヾ` by auto()
    val `(⊃д⊂)` by auto()
    val `(ノдヽ)` by auto()
    val `(⊃‿⊂)` by auto("(⊃‿⊂)")
    val `(／ω＼)` by auto("(/ω＼)")
    val `(／。＼)` by auto()
    val `（／｡＼)` by auto("（/｡＼)")
    val `（／．＼）` by auto()
    val `（／_＼）` by auto()
    val `(゜Д゜*)` by auto()
    val `(ﾟДﾟ;)` by auto("(ﾟДﾟ;)")
    val `§;ﾟﾛﾟ§` by auto("§;ﾟﾛﾟ§")
    val `(꒪ȏ꒪;)` by auto("(꒪ȏ꒪;)")
    val `(☼Д☼)` by auto()
    val `( ﾉД`)` by auto()
    val `(-@Д@)` by auto()
    val `(ﾟДﾟ；∬` by auto()
    val `⊂（゜Д゜⊂` by auto()
    val `⊃゜Д゜）⊃` by auto()
    val `（´皿｀；）` by auto()
    val `(＠O＠;)` by auto("(＠O＠;)")
    val `( ⁰д⁰)` by auto()
    val `) ゜o゜(` by auto()
    val `(ノ′Дヾ)` by auto()
    val `(　〇□〇）` by auto()
    val `(／;◇;)／` by auto("(/;◇;)/")
    val `џ(ºДºџ)` by auto()
    val `ヽ(ﾟДﾟ)ﾉ` by auto()
}
