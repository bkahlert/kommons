@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Smile : Category() {
    val `＾ω＾` by auto()
    val `▼ω▼` by auto()
    val `(´∀｀）` by auto()
    val `（＾∀＾）` by auto()
    val `(^○^)` by auto()
    val `(｀▽´)` by auto()
    val `米＾－＾米` by auto()
    val `(∩_∩)` by auto()
    val `(⌒∇⌒)` by auto()
    val `(☆▽☆)` by auto()
    val `(￣∇￣)` by auto()
    val `（￣ー+￣）` by auto()
    val `σ(^○^)` by auto()
    val `(^■^*)` by auto()
    val `(*^-^)` by auto()
    val `d(^^*)` by auto()
    val `(´w｀*)` by auto()
    val `|*￣ー￣|` by auto()
    val `{*≧∀≦}` by auto()
    val `(o^^o)` by auto()
    val `(o^∀^)` by auto()
    val `(*´ー`)` by auto("(*´ー`)")
    val `o(^▽^)o` by auto()
    val `o(^-^)o` by auto()
    val `(*´∇｀*)` by auto()
    val `（*´▽`*)` by auto("（*´▽`*)")
    val `(=^_^=)` by auto()
    val `σ(ﾟｰ^*)` by auto()
    val `（●＞ω＜●）` by auto()
    val `y（^ヮ^）y` by auto()
}
