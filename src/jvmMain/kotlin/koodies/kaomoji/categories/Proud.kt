@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Proud : Category() {
    val `（咒）` by auto()
    val `(⌤⌗)` by auto()
    val `(￣^￣)` by auto()
    val `(-、-)` by auto()
    val `(꒵꜅꒵)` by auto()
    val `(｀へ′)` by auto()
    val `（’へ’）` by auto()
    val `╭⚈¬⚈╮` by auto()
    val `(￣ω￣)` by auto()
    val `(｀ڼ´)` by auto()
    val `⚈ ̫ ⚈` by auto()
    val `（￣へ￣）` by auto()
    val `（￣＾￣）` by auto()
    val `(´꒳`)` by auto("(´꒳`)")
    val `(꒡ꜙ꒡)` by auto()
    val `(^～^)` by auto()
    val `(´Å｀)` by auto()
    val `§ԾᴗԾ§` by auto()
    val `（￣ー￣）` by auto()
    val `(｀^´)` by auto()
    val `（・―・）` by auto()
    val `(￣‥￣)` by auto()
    val `(￣ー￣)` by auto()
    val `（｀ー´）` by auto()
    val `(￣⊿￣)` by auto()
    val `(-∀-)` by auto()
    val `( ´ｰ`)` by auto("( ´ｰ`)")
    val `（；￣︶￣）` by auto("（；￣︶￣）")
    val `(￣︶￣;)` by auto("(￣︶￣;)")
    val `( ⊙‿⊙)` by auto()
}
