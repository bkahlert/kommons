@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Tired : Category() {
    val `ＯＴＬ` by auto()
    val `ｏｒｚ` by auto()
    val `＿ﾉフ○` by auto()
    val `(￣Д￣` by auto()
    val `(´Д⊂` by auto()
    val `（－－；` by auto()
    val `（´。｀)` by auto()
    val `（￣Ω￣）` by auto()
    val `(A´Å｀` by auto()
    val `A^-^)` by auto()
    val `(∋д∈)` by auto()
    val `(・。-;` by auto("(・。-;")
    val `（´・｀）` by auto()
    val `(*Q*)` by auto()
    val `(／０￣)` by auto()
    val `＿|￣|○` by auto()
    val `(-。-;` by auto("(-。-;")
    val `(´Д｀)` by auto()
    val `(;´Д｀)` by auto("(;´Д｀)")
    val `（＾。＾；）` by auto()
    val `（´＿｀;）` by auto("（´＿｀;）")
    val `（；￣д￣）` by auto()
    val `（´ヘ｀；）` by auto()
    val `(´Д｀)ヾ` by auto()
    val `(　〇□〇）` by auto()
    val `(´ヘ｀;)` by auto("(´ヘ｀;)")
    val `(;・・;)` by auto("(;・・;)")
    val `（○o●;）` by auto("（○o●;）")
    val `(´皿｀;)` by auto("(´皿｀;)")
    val `(*ﾉω-)` by auto("(*ﾉω-)")
}
