@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Laugh : Category() {
    val `(खਉख)` by auto()
    val `(ಡ艸ಡ)` by auto()
    val `（⌒▽⌒）` by auto()
    val `（＾ｖ＾）` by auto()
    val `（＞ｙ＜）` by auto()
    val `(＾艸＾)` by auto()
    val `(Ŏ艸Ŏ)` by auto()
    val `(亝艸亝)` by auto()
    val `(♡´艸`)` by auto("(♡´艸`)")
    val `(●´艸`)` by auto("(●´艸`)")
    val `(≧艸≦*)` by auto()
    val `(*＞艸＜)` by auto("(*>艸<)")
    val `( ਊдਊ)` by auto()
    val `( ´艸｀)` by auto()
    val `(｡ˇ艸ˇ)` by auto()
    val `(o＞艸＜)` by auto("(o>艸<)")
    val `(^ц^ )` by auto()
    val `(☆Θ艸Θ)` by auto()
    val `（○´艸｀）` by auto()
    val `(*´艸｀)` by auto()
    val `（≧ｙ≦＊）` by auto()
    val `(*・艸・)` by auto()
    val `（　´∀｀）` by auto()
    val `(*≧艸≦)` by auto()
    val `( ╹ਊ╹)` by auto()
    val `( ՞ਊ՞)` by auto()
    val `(*´∀｀）` by auto()
    val `(✪ฺܫ✪ฺ)` by auto()
    val `( 〃´艸｀)` by auto()
    val `(❤ฺ￫艸￩)` by auto()
}
