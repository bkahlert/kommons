@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Hug : Category() {
    val `⊂（♡⌂♡）⊃` by auto()
    val `⊂(◉‿◉)つ` by auto()
    val `ლ(・ヮ・ლ)` by auto()
    val `(つ◉益◉)つ` by auto()
    val `⊂(・﹏・⊂)` by auto()
    val `⊂(・ヮ・⊂)` by auto()
    val `⊂(・▽・⊂)` by auto()
    val `(っಠ‿ಠ)っ` by auto()
    val `ʕっ•ᴥ•ʔっ` by auto()
    val `（っ・∀・）っ` by auto()
    val `(っ⇀⑃↼)っ` by auto()
    val `＼(^o^)／` by auto()
    val `(つ´∀｀)つ` by auto()
    val `(つ▀¯▀)つ` by auto()
    val `(っ´▽｀)っ` by auto()
    val `ლ(・﹏・ლ)` by auto()
    val `⊂(･ω･*⊂)` by auto()
    val `ლ(･ω･*ლ)` by auto()
    val `(づ￣ ³￣)づ` by auto()
    val `ლ(´ ❥ `ლ)` by auto("ლ(´ ❥ `ლ)")
    val `c⌒っ╹v╹ )っ` by auto()
    val `d=(´▽｀)=b` by auto()
    val `⊂( ◜◒◝ )⊃` by auto()
    val `(․づ◡﹏◡)づ․` by auto("(.づ◡﹏◡)づ.")
    val `(っ*´∀｀*)っ` by auto()
    val `(っ⇀`皿′↼)っ` by auto("(っ⇀`皿′↼)っ")
    val `(․づσ▿σ)づ․` by auto("(.づσ▿σ)づ.")
    val `⊂(´・ω・｀⊂)` by auto()
    val `(*￣-(-*)ゝ` by auto()
    val `⊂((・▽・))⊃` by auto()
}
