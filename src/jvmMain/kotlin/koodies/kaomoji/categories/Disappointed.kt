@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Disappointed : Category() {
    val `orz` by auto("orz")
    val `(´Д⊂` by auto("(´Д⊂")
    val `（πーπ）` by auto("（πーπ）")
    val `(´A`)` by auto("(´A`)")
    val `(ન_੦)` by auto()
    val `(´д｀)` by auto()
    val `（；＿；）` by auto()
    val `(´ω｀)` by auto()
    val `(´ж｀;)` by auto("(´ж｀;)")
    val `つ´Д`)つ` by auto("つ´Д`)つ")
    val `（ノд｀＠）` by auto()
    val `从´_υ｀从` by auto()
    val `ヽ(冫、)ﾉ` by auto()
    val `(i々i)］` by auto()
    val `(;´д｀)` by auto("(;´д｀)")
    val `(ノ_＜。)` by auto("(ノ_<。)")
    val `( p_q)` by auto()
    val `(´･ω･`)` by auto("(´･ω･`)")
    val `o(；△；)o` by auto()
    val `( ｉдｉ )` by auto()
    val `(´・ω・｀)` by auto()
    val `(*ノ_＜*)` by auto("(*ノ_<*)")
    val `(´；ω；｀)` by auto()
    val `ヽ(´Д`;)ﾉ` by auto("ヽ(´Д`;)ﾉ")
    val `(；一ω一||)` by auto()
    val `(_　_|||)` by auto()
    val `Σ(￣ロ￣lll)` by auto()
    val `＿ﾉ乙(､ﾝ､)＿` by auto()
    val `／／／orz／／／` by auto("///orz///")
    val `（（（(T-T*)）））` by auto()
}
