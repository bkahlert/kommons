@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Grin : Category() {
    val `（´υ｀）` by auto()
    val `(￣▼￣)` by auto()
    val `(ﾟｰﾟ)` by auto()
    val `(´ｖ｀)` by auto()
    val `(･∀･)` by auto()
    val `（☆∀☆）` by auto()
    val `(￣ー￣)` by auto()
    val `(・∀・)` by auto()
    val `(｀ω´)` by auto()
    val `(　￣ー)` by auto()
    val `┫￣旦￣┣` by auto()
    val `Ｕ^皿^Ｕ` by auto()
    val `（＿ー＿）` by auto()
    val `(▼∀▼)` by auto()
    val `（■皿■）` by auto()
    val `（￣ー＋￣）` by auto()
    val `(☆＞⊇＜)` by auto("(☆>⊇<)")
    val `｜*￣∇￣｜` by auto()
    val `(￣▼￣*)` by auto()
    val `(｡-∀-)` by auto()
    val `（*ﾟーﾟ）` by auto()
    val `v(￣∇￣)` by auto()
    val `（＿ー+＿）` by auto()
    val `（￣+ー￣）` by auto()
    val `(´、ゝ｀)` by auto()
    val `（´－｀）ノ` by auto()
    val `(・∀・｀；)` by auto()
    val `(　´_ゝ`)` by auto()
    val `(#~皿~#)` by auto()
    val `（￣￣ー￣￣）` by auto()
}
