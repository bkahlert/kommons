@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object BadMood : Category() {
    val `（￣s￣；` by auto()
    val `（￣ε￣）` by auto()
    val `( ￣＾￣)` by auto()
    val `(´ﾟзﾟ)` by auto()
    val `(；´д`)` by auto("(;´д`)")
    val `（＞μ＜＃）` by auto()
    val `(´･з･)` by auto()
    val `(`ε´*)` by auto()
    val `(ｰ ｰ；)` by auto("(ｰ ｰ;)")
    val `(´･･`)` by auto("(´･･`)")
    val `(ó﹏ò｡)` by auto()
    val `(・ε・｀*)` by auto()
    val `(´･д･｀)` by auto()
    val `…φ(。。*)` by auto()
    val `(。・ε・。)` by auto()
    val `(´･_･｀)` by auto()
    val `( *｀з´)` by auto()
    val `(*･ε･*)` by auto()
    val `( *ﾟзﾟ)` by auto()
    val `(´-ε-｀)` by auto()
    val `(´･ε･｀)` by auto()
    val `(-公- ；)` by auto("(-公- ;)")
    val `(・´з`・)` by auto("(・´з`・)")
    val `(・ε・`*)` by auto("(・ε・`*)")
    val `(*・ε・*)` by auto()
    val `(･ε･｀ )` by auto()
    val `(=`,´=)` by auto("(=｀.´=)")
    val `(￣ε(#￣)` by auto()
    val `(´･ｪ･｀)` by auto()
    val `(●´^｀●)` by auto()
}
