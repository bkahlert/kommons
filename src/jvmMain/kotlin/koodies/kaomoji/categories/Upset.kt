@file:Suppress(
    "PublicApiImplicitType",
    "KDocMissingDocumentation",
    "ObjectPropertyName",
    "RemoveRedundantBackticks",
    "unused",
    "NonAsciiCharacters",
    "SpellCheckingInspection",
    "DANGEROUS_CHARACTERS"
)


package koodies.kaomoji.categories

import koodies.kaomoji.Category

object Upset : Category() {
    val `(＾＾；)` by auto()
    val `（￣ー￣；` by auto()
    val `(⌒_⌒;` by auto("(⌒_⌒;")
    val `(＞△＜Uu` by auto()
    val `(;･∀･)` by auto("(;･∀･)")
    val `（　´д｀）` by auto()
    val `(；￣ー￣A` by auto()
    val `(；´艸｀)` by auto()
    val `(；゜○゜)` by auto()
    val `(´∀｀；A` by auto()
    val `｜￣ω￣A；` by auto()
    val `（；・д・）` by auto()
    val `(´Д｀ι)` by auto()
    val `ヽ(´o｀；` by auto()
    val `（；￣Д￣）` by auto()
    val `（´ヘ｀；）` by auto()
    val `(┼д┼;)` by auto("(┼д┼;)")
    val `（-Ⅱ-；）` by auto()
    val `(x□ｘ｀;)` by auto("(x□ｘ｀;)")
    val `(;′Д`)ノ` by auto("(;′Д`)ノ")
    val `(￣ー￣;)ゞ` by auto("(￣ー￣;)ゞ")
    val `σ(￣∇￣;)` by auto("σ(￣∇￣;)")
    val `(≧mm≦;)` by auto("(≧mm≦;)")
    val `(*ﾉω＜*)` by auto("(*ﾉω<*)")
    val `ε(`∞`*)` by auto()
    val `ε-(´・｀)` by auto()
    val `ヾ(￣o￣;)` by auto("ヾ(￣o￣;)")
    val `ヾ(°∇°*)` by auto()
    val `(●′Д`●)` by auto()
    val `(；´д｀)ゞ` by auto()
}
